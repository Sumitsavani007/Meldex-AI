# AI Studio Theme Contrast Fix Report

Date: 2026-06-28

## Fixes

- Prompt textarea now explicitly uses readable text and placeholder colors.
- Search input now uses readable text and placeholder colors.
- Selects use dark color scheme so option values remain visible.
- Numeric and negative prompt fields now explicitly use readable text.
- Disabled and loading states are visible.

## Notes

AI Studio keeps a premium dark media-production canvas inside the user shell, so it remains readable when the global app is in light or dark mode.
